-- ─────────────────────────────────────────────────────────────────────────────
-- 웰니스코인 엑셀(선불 관리자 거래 요청 양식) 발급용 고객아이디 컬럼 추가
-- Supabase Dashboard > SQL Editor 에서 실행하세요 (몇 번 실행해도 안전 · idempotent)
-- ─────────────────────────────────────────────────────────────────────────────

-- 직원 등록/수정 화면에서 입력하는 고객아이디. 웰니스코인 엑셀 C열에 그대로 들어간다.
-- nullable(기본값 없음)이라 기존 직원 데이터는 전혀 영향받지 않고 그대로 NULL로 유지된다.
alter table public.employees
  add column if not exists customer_id text;

comment on column public.employees.customer_id is
  '웰니스코인 지급 시 사용하는 고객아이디(선불 관리자 거래 요청 양식 C열). 값이 없으면 해당 직원은 웰니스코인 엑셀 다운로드 대상에서 제외(에러 표시)됨. 기존 직원 데이터/메일 이력에는 영향 없음.';
